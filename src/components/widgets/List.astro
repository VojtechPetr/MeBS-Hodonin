---
const { title, items, class: extraClass } = Astro.props;  
---

<div class="mb-8">
  {title && <h2 class="text-2xl font-semibold mb-4">{title}</h2>}
  <ul class={`list-disc list-inside space-y-2 ${extraClass}`}> 
    {items.map(item => (
      <li>{item}</li>
    ))}
  </ul>
</div>
